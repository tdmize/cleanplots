% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanplots_defaults.R
\name{cleanplots_defaults}
\alias{cleanplots_defaults}
\title{Set cleanplots defaults for the session}
\usage{
cleanplots_defaults(
  base_size = 12,
  point_size = 1.4,
  point_stroke = 0.7,
  line_width = 0.65,
  smooth_color = "#D50000"
)
}
\arguments{
\item{base_size}{Base font size in points passed to
\code{\link[=theme_cleanplots]{theme_cleanplots()}} (default: \code{12}).}

\item{point_size}{Default size for \code{geom_point()} markers
(default: \code{1.4}). The midpoint dot of \code{geom_pointrange()} (used in
coefficient plots) is sized to match.}

\item{point_stroke}{Default outline thickness for markers, which
controls how visible the hollow shapes are (default: \code{0.7};
ggplot2's own default is 0.5).}

\item{line_width}{Default line width for \code{geom_line()}, \code{geom_path()},
\code{geom_step()}, \code{geom_density()}, \code{geom_function()}, and
\code{geom_smooth()} (default: \code{0.65}; ggplot2's own default is 0.5).
Error bars, line ranges, and point ranges are set to 80\% of this
value. Reference lines (\code{geom_hline()}, \code{geom_vline()},
\code{geom_abline()}) are deliberately left thin and unobtrusive.}

\item{smooth_color}{Default line color for \code{geom_smooth()}
(default: cleanplots red, \code{"#D50000"}).}
}
\value{
Invisibly returns \code{NULL}; called for its side effects.
}
\description{
One call makes ggplot2 behave like the Stata cleanplots scheme for the
rest of the session: sets \code{\link[=theme_cleanplots]{theme_cleanplots()}} as the default theme,
makes points, markers, and lines larger and thicker (matching the
scheme's more visible defaults), and registers the cleanplots color and
fill scales as the session defaults so plots are colored correctly
without adding scales by hand.
}
\details{
After calling \code{cleanplots_defaults()}:
\itemize{
\item All plots use \code{theme_cleanplots()} unless another theme is added.
\item \code{geom_point()} draws larger markers with a heavier outline
(important for the hollow marker shapes).
\item \code{geom_line()} and friends draw thicker lines so colors are
visible.
\item \code{geom_smooth()} fit lines default to cleanplots red with a
light gray confidence band (instead of ggplot2's blue).
\item Discrete \code{color} aesthetics default to the main cleanplots
palette, and discrete \code{fill} aesthetics default to the softer bar
palette (as in Stata, where bars and areas automatically use the
softer colors). Add a scale explicitly to override, e.g.
\code{scale_fill_cleanplots(palette = "default")}.
}

These are session-wide settings that affect all subsequent ggplot2
plots. To undo them, restart R or call \code{ggplot2::theme_set(ggplot2::theme_gray())},
\code{ggplot2::update_geom_defaults()}, and \code{options(ggplot2.discrete.colour = NULL, ggplot2.discrete.fill = NULL)}.
}
\examples{
library(ggplot2)
cleanplots_defaults()

# No scales or theme needed -- everything is applied by default
ggplot(iris, aes(Sepal.Length, Sepal.Width, color = Species)) +
  geom_point()
}
