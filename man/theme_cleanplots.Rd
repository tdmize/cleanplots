% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_cleanplots.R
\name{theme_cleanplots}
\alias{theme_cleanplots}
\title{cleanplots theme for ggplot2}
\usage{
theme_cleanplots(base_size = 12, base_family = "", legend_position = "right")
}
\arguments{
\item{base_size}{Base font size in points (default: \code{12}).}

\item{base_family}{Base font family (default: \code{""}).}

\item{legend_position}{Position of the legend (default: \code{"right"},
bottom-justified, matching the Stata scheme's 4-o'clock position).
Set to \code{"none"} to remove, or any value accepted by
\code{ggplot2::theme(legend.position = ...)}.}
}
\value{
A ggplot2 theme object.
}
\description{
A ggplot2 theme that replicates the non-color settings of the Stata
\code{cleanplots} graphing scheme: white background, no plot-region border,
light gray (gs10) axis lines and ticks, dotted light gray horizontal and
vertical gridlines, and a frameless legend in the lower right.
}
\details{
Correspondence to the Stata scheme file:
\itemize{
\item White background (\verb{color background white})
\item No plot region border (\verb{linestyle plotregion none})
\item Axis lines, ticks in gs10 gray (\verb{color axisline gs10}, etc.)
\item Dotted gs10 major gridlines, horizontal and vertical
(\verb{draw_major_hgrid yes}, \verb{draw_major_vgrid yes},
\verb{linepattern major_grid dot}, \verb{color major_grid gs10})
\item No minor gridlines
\item Legend at lower right, single column, no frame, no title
(\verb{clockdir legend_position 4}, \verb{numstyle legend_cols 1},
\verb{linestyle legend none})
\item Horizontal y-axis tick labels (\verb{anglestyle vertical_tick horizontal})
}
}
\examples{
library(ggplot2)

ggplot(iris, aes(x = Sepal.Length, y = Sepal.Width, color = Species)) +
  geom_point(size = 2) +
  theme_cleanplots() +
  scale_color_cleanplots()
}
